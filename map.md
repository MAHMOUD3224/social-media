# Our Map
### check if user is logged in or not when he wants to add comment

### Check the browser language. If the language is English, the direction is LTR, and vice versa if the language is Arabic.

### when User Click To Re-post you should show two cases if he want re-post with no words or re-post with type something.

### When users click to comment, you need to open the comment container and count any comments they have posted.

### If the Users click to Share, you must create a Template for anyone who opens the link to take them to the post that the user shared

### Add two buttons in the User's post <button>Update</button> and <button>Delete</button> 

### use cloudFlare to host your images, videos, likes, views

### we must update the user post count and comment count cuz in localStorage we have the user data and we can get the post count and comment count from it but we need to update it in the UI, so how do we do that? we can use the `DOMContentLoaded` event to update the UI when the page loads.
-----

## Done

- ### show alert in every status like logout,login,SignUp,Add Post, Add Comment.

- ### When the user registers or logs in, display the user's picture and name instead of registration buttons and show  
<button style='background:#ff6b79; border:none; padding:5px;'>logout button </button> 

- When the user clicks on the image post, a box opens with an image
-----



## in Progress

-----

## in future inshallah
- Create tags if the user selects this option.
- Create a view counter for each post to display the number of views.
- make sound by js when User hover buttons
- Allow the user to post more than one photo or video. 
-----


## The new knowledge I have gained
> ### closest("SomeElement")

> ###  new DataTransfer() 

> "Content-Type":"multipart/form-data"

> ### let formData = new FormData();
>        formData.append("username", usernameRegister.value);
>        formData.append("password", passwordRegister.value);
>        formData.append("name", nameRegister.value);

> ### onerror

> ### any steps you didn't do write comment start with 'todo:'

> ### when you drop file in input-file `type ="file"` there is events for this input `dragover` & `dragleave` & `drop` 
----

## Questions that I am curious to know the answers to

1 - what is the difference between `Authorization` and `Authentication`?

2 - What is requestAnimationFrame in Js

3 - what is Intersection new IntersectionObserver

----

## Notes/Updates
### create Elements in js not with `innerHTML`
### revise the "module" lesson in elzero web school course



# profile page


## in progress
- ### when the user clicks on the profile image, it opens a box with the user's image

- ### open plan page when the user clicks on Get Verified button

- ### when the user clicks on the Set up profile button, it opens a box available to change the img and name

- ### create Qr code for the user profile to share it with others

---